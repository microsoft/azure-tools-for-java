dependencies {
    compile rootProject
    testCompile rootProject
}

intellij {
    pluginName = 'azure-toolkit-for-rider'
    version = rider_version
}

instrumentCode {
    javac2(file("libs/javac2.jar"))
}